"use client";

const DashboardToggler = () => {
  return (
    <div
      className="absolute top-14 -right-8 w-12 h-8 rounded-lg z-50 bg-black"
      onClick={() => console.log("clivked")}
    />
  );
};

export default DashboardToggler;
